/**
 * 全局布局常量：TopNav、Layout、Detail 等共用，避免 magic number 不一致。
 */

/** 主内容区水平内边距（Chakra space，如 6 = 1.5rem） */
export const LAYOUT_GUTTER = 6;

/** TopNav 视觉高度对应的 Chakra space，用于 Detail 页 extrafanart 负 margin 对齐 */
export const TOPNAV_HEIGHT_SPACE = 16;
